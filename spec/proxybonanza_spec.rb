require 'spec_helper'

describe Proxybonanza do
  it 'has a version number' do
    expect(Proxybonanza::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
